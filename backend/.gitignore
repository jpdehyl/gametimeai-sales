node_modules
# Keep environment variables out of version control
.env

# Prisma generated client (regenerated via `npx prisma generate`)
/src/generated/prisma

# SQLite database file (regenerated via `npx prisma db seed`)
*.db
*.db-journal
